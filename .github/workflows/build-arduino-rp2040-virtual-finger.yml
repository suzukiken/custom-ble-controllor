name: Build Arduino RP2040 virtual-finger

on:
  push:
    paths:
      - "arduino/xiao-rp2040/virtual-finger/**"
      - ".github/workflows/build-arduino-rp2040-virtual-finger.yml"
  pull_request:
    paths:
      - "arduino/xiao-rp2040/virtual-finger/**"
      - ".github/workflows/build-arduino-rp2040-virtual-finger.yml"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup arduino-cli
        uses: arduino/setup-arduino-cli@v2

      - name: Install RP2040 core
        run: |
          arduino-cli config init
          arduino-cli config add board_manager.additional_urls \
            https://github.com/earlephilhower/arduino-pico/releases/download/global/package_rp2040_index.json
          arduino-cli core update-index
          arduino-cli core install rp2040:rp2040

      - name: Compile UF2
        run: |
          mkdir -p out
          arduino-cli compile \
            --fqbn rp2040:rp2040:seeed_xiao_rp2040 \
            --output-dir out \
            arduino/xiao-rp2040/virtual-finger
          ls -la out
          UF2=$(find out -name '*.uf2' | head -n 1)
          test -n "$UF2"
          cp "$UF2" out/xiao-rp2040-virtual-finger.uf2

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: arduino-rp2040-virtual-finger
          path: out/xiao-rp2040-virtual-finger.uf2
